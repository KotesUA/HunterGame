namespace Assets.Scripts.Enums
{
    public enum HorizontalState
    {
        Idle,
        Right,
        Left
    }

    public enum VerticalState
    {
        Idle,
        Up,
        Down
    }
}
